const initialState = {}
const event = (state = initialState, action) => {
    switch (action.type) {
        case "HANDLE_FORM_CHANGE":
            return state;
            break;

        default:
            break;
            return state;
    }
}

export default event;